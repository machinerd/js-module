import { Meta, Title, Primary, Controls, Markdown, Canvas } from '@storybook/addon-docs/blocks';
import * as DialogStories from './dialog.stories';

<Meta of={DialogStories} />

<Title />

<Markdown>
  {`
클라이언트 전용 오버레이 다이얼로그입니다. \`isOpen\` / \`onClose\`로 열고 닫습니다.

- **열기**: 보통 트리거 버튼의 \`onClick\`에서 \`isOpen\`을 \`true\`로 둡니다.
- **닫기**: \`closeOnBackdropClick\`이 켜져 있으면 **반투명 배경** 클릭 시 \`onClose\`가 호출되고, **Esc**로도 닫힙니다. 패널 안의 버튼에서 \`onClose\`를 호출하는 패턴도 흔합니다.
- **스크롤**: 열려 있을 때 \`document.body\`의 \`overflow\`를 막습니다.
- **애니메이션**: \`useDelayUnmount\`로 닫힌 뒤에도 짧은 시간 DOM에 남아 페이드아웃할 수 있습니다.

아래 **Playground**는 \`Button\`으로 다이얼로그를 연 뒤, 배경·Esc·패널 버튼으로 닫는 흐름입니다.
`}
</Markdown>

## Playground

<Primary of={DialogStories.Playground} />

<Controls />

## 배경 클릭으로 닫기 비활성

<Canvas of={DialogStories.NoBackdropClose} />

import { Meta, Title, Primary, Controls, Markdown, Canvas } from '@storybook/addon-docs/blocks';
import * as SkeletonStories from './skeleton.stories';

<Meta of={SkeletonStories} />

<Title />

<Markdown>
  {`
로딩 플레이스홀더용 블록입니다. \`size\`는 높이 프리셋, \`width\` / \`height\`로 픽셀 또는 CSS 문자열을 줄 수 있습니다.
`}
</Markdown>

<Primary of={SkeletonStories.Playground} />

<Controls />

## 카드형 플레이스홀더

<Canvas of={SkeletonStories.CardPlaceholder} />

## 아바타형

<Canvas of={SkeletonStories.AvatarPlaceholder} />

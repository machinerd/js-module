import { Meta, Title, Primary, Controls, Markdown, Canvas } from '@storybook/addon-docs/blocks';
import * as ButtonStories from './button.stories';

<Meta of={ButtonStories} />

<Title />

<Markdown>
  {`
CVA 기반 버튼입니다. \`outline\`과 \`variant\` 조합에 따라 스타일이 결정됩니다.

- **Playground**: 컨트롤로 크기·테두리·색·비활성 등을 바로 확인합니다.
- **조합 갤러리**: 지원하는 outline/variant 조합을 한 화면에서 봅니다.
- **asChild 링크**: Radix Slot으로 \`<a>\` 등 자식에 스타일만 입힙니다.
`}
</Markdown>

## Playground

<Primary of={ButtonStories.Playground} />

<Controls />

## 조합 갤러리

<Canvas of={ButtonStories.VariantsGallery} />

## asChild 링크

<Canvas of={ButtonStories.AsLink} />

import { Meta, Title, Primary, Controls, Markdown } from '@storybook/addon-docs/blocks';
import * as ConditionalWrapperStories from './conditional-wrapper.stories';

<Meta of={ConditionalWrapperStories} />

<Title />

<Markdown>
  {`
\`condition\`이 참일 때만 \`wrapper\`로 children을 감쌉니다. \`wrapper\`는 컨트롤에 없고, 스토리마다 다른 함수를 넘기는 방식으로 예시를 둡니다.
`}
</Markdown>

<Primary />

<Controls />

import { Meta, Title, Primary, Controls, Markdown, Canvas } from '@storybook/addon-docs/blocks';
import * as TooltipStories from './tooltip.stories';

<Meta of={TooltipStories} />

<Title />

<Markdown>
  {`
클라이언트 전용 툴팁입니다. 트리거에 호버·포커스 시 내용이 표시되고, 바깥 클릭 시 닫힙니다.

\`placement\`는 **top / bottom** 과 **start / end** 정렬(총 6가지)만 지원합니다.
`}
</Markdown>

<Primary of={TooltipStories.Playground} />

<Controls />

## 배치 미리보기

<Canvas of={TooltipStories.PlacementGrid} />
